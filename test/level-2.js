const {test} = require('ava')

// map
// filter
// every
// fill
// keys
// values
// reverse
// toString

test.todo('map() throws error if no callback passed in')

test.todo('map(fn) returns new array with the same length')

test.todo('map(fn) returns new array with the fn(element) returned value as elements')

test.todo('filter() throws error if no callback passed in')

test.todo('filter(test) returns new array that contains only elements that passes test(element)')

test.todo('filter(test) returns new empty array on empty array')

test.todo('filter(test) returns new array if all elements passes test(element)')

test.todo('every() throws error if no callback passed in')

test.todo('fill() returns array with all elements as undefined')

test.todo('fill(value) returns array with all elements as value')

test.todo('keys()')

test.todo('values()')

test.todo('reverse()')

test.todo('toString()')
