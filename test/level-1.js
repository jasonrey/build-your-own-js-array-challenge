const {test} = require('ava')

// push
// pop
// shift
// unshift
// join
// forEach

test.todo('push(element) adds element to the end of the array')

test.todo('push(element) increases the length of the array by 1')

test.todo('push(...element) add multiple elements to the end of the array')

test.todo('push(...element) increases the length of the array by ...element.length')

test.todo('pop() returns the last element of the array')

test.todo('pop() removes the last element of the array')

test.todo('pop() decreases the length of the array')

test.todo('pop() returns undefined for empty array')

test.todo('shift() returns the first element of the array')

test.todo('shift() removes the first element of the array')

test.todo('shift() decreases the length of the array')

test.todo('shift() returns undefined for empty array')

test.todo('unshift(element) adds element to the beginning of the array')

test.todo('unshift(element) increases the length of the array by 1')

test.todo('unshift(...element) add multiple elements to the beginning of the array')

test.todo('unshift(...element) increases the length of the array by ...element.length')

test.todo('join() returns string with all elements joined by comma as delimeter')

test.todo('join() returns empty string for empty array')

test.todo('join(delimeter) returns string with all elements joined by delimeter')

test.todo('forEach() throws error if no callback passed in')

test.todo('forEach(fn) returns undefined')

test.todo('forEach(fn) iterates over the array')

test.todo('forEach(fn) iterates over the array with index')
